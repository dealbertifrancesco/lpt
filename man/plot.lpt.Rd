% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.lpt}
\alias{plot.lpt}
\title{Plot LPT results}
\usage{
\method{plot}{lpt}(
  x,
  type = c("datt", "att", "pretrends", "sensitivity"),
  period = NULL,
  d0 = NULL,
  B_grid = NULL,
  true_curve = NULL,
  ...
)
}
\arguments{
\item{x}{An object of class \code{"lpt"}.}

\item{type}{Character. Plot type: \code{"datt"} (default), \code{"att"},
\code{"pretrends"}, \code{"sensitivity"}.}

\item{period}{Scalar or NULL. Which post-period to plot. If NULL and
multiple post-periods exist, plots all periods faceted. Default: NULL.}

\item{d0}{Numeric or NULL. Dose value for the sensitivity plot.
Default: median of evaluation grid.}

\item{B_grid}{Numeric vector or NULL. Grid of B values for sensitivity plot.
Default: \code{seq(0, 2 * B_hat, length.out = 50)}.}

\item{true_curve}{Function or NULL. If provided, a function \code{f(d)} that
returns the true value at dose \code{d}, overlaid as a dashed black line.
Applies to \code{"datt"} and \code{"att"} plot types only.}

\item{...}{Additional arguments (unused).}
}
\value{
A \code{ggplot} object (invisibly).
}
\description{
Creates visualizations for identified sets, pre-trend diagnostics,
and sensitivity analysis.
}
\examples{
data(sru)
fit <- lpt(sru, "commune", "year", "outcome", "dose",
           post_period = 2019, pre_periods = 1993:1999, B = 0)
if (requireNamespace("ggplot2", quietly = TRUE)) {
  plot(fit, type = "datt")
}

}
