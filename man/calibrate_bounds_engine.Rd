% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibrate_bounds.R
\name{calibrate_bounds_engine}
\alias{calibrate_bounds_engine}
\title{Calibration engine shared across estimation backends}
\usage{
calibrate_bounds_engine(
  data,
  id_col,
  time_col,
  outcome_col,
  dose_col,
  pre_periods,
  eval_points,
  fit_fun
)
}
\arguments{
\item{data, id_col, time_col, outcome_col, dose_col}{As in
\code{\link{calibrate_bounds}}.}

\item{pre_periods}{Sorted vector of pre-period identifiers (>= 2).}

\item{eval_points}{Numeric vector or NULL. If NULL, a default grid over
the 5th-95th percentile of positive doses in the first pair is used.}

\item{fit_fun}{Function \code{(delta_y, dose, eval_points)} returning a
list with elements \code{mean} (fitted conditional mean on the grid)
and \code{deriv} (its derivative on the grid).}
}
\value{
Same structure as \code{\link{calibrate_bounds}}.
}
\description{
Iterates over consecutive pre-period pairs, fits the selection function
\eqn{\hat{\mu}_s} on treated units via the supplied fitter, and computes
the level deviations (against the untreated sample mean) and slopes that
calibrate M and B.
}
\keyword{internal}
